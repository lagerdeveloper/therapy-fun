//This reducer should map a gameID to an array of difficulty levels for that game { 1: [{}, {}] }
export default (state = {}, action) => {
  switch(action.type) {
    default:
      return state;
  }
}
